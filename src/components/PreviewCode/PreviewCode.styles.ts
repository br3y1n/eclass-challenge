import { Styles } from "../../models";
import { fonts } from "../../themes/theme";

const previewCodeStyles: Styles = {
  code: {
    my: 2,
    textAlign: "left",
    fontSize: {
      xs: fonts.body1.xs,
      sm: fonts.body1.sm,
    },
  },
};

export { previewCodeStyles };
